# Test-only overrides. Production defaults in src/main stay empty so a
# missing VPS env var cannot ship placeholder contacts or a fake UPI VPA.
store.upi-id=gpstore-test@upi
store.support-phone=+919876543210
store.support-email=test-support@gpstore.co.in
